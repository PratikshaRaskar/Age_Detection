# Edge_Detection
The main aim of this project is to classify human faces into different age groups  using images. The model is trained on a dataset of facial images, where each image  is labeled with an age range. By learning patterns such as wrinkles, facial structure,  and skin texture, the CNN model can accurately predict the age category of a  person.
